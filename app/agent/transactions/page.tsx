import TransactionWorkspace from '@/components/agent/TransactionWorkspace';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function TransactionsPage() {
  return <TransactionWorkspace />;
}
