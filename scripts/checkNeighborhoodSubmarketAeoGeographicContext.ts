import assert from 'node:assert/strict';import{evaluateGeographicContentAuthority,GEOGRAPHIC_CONTENT_AUTHORITY_STATUS}from'../lib/neighborhood-submarket/geographicContentAuthority';import{GEOGRAPHIC_CONTENT_AUTHORITY_FIXTURES as f}from'../lib/neighborhood-submarket/geographicContentAuthorityFixtures';const r=Object.fromEntries(Object.entries(f).map(([k,v])=>[k,evaluateGeographicContentAuthority(v)]));assert.equal(GEOGRAPHIC_CONTENT_AUTHORITY_STATUS,'IMPLEMENTED_INTERNAL_ARCHITECTURE_ONLY');for(const x of Object.values(r)){assert.equal(x.publication,false);assert.equal(x.activationState,'NOT_AUTHORIZED')}assert.equal(r.niwot.visibility,'NOT_READY');assert.equal(r.gunbarrel.visibility,'NOT_READY');assert.equal(r.tableMesa.visibility,'NOT_READY');assert.equal(r.prohibited.visibility,'COMPLIANCE_BLOCKED');assert(r.publication.reasons.includes('PUBLICATION_NOT_AUTHORIZED'));console.log('NEIGHBORHOOD_SUBMARKET_AEO_GEOGRAPHIC_CONTEXT_CHECK: PASS');
