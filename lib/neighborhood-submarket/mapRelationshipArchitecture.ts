export const MAP_RELATIONSHIP_ARCHITECTURE_STATUS = 'IMPLEMENTED_INTERNAL_ARCHITECTURE_ONLY' as const;
export type MapDisplayRole = 'ORIENTATION_ONLY_POINT'|'PROVIDER_REPORTED_COORDINATE'|'VERIFIED_PROPERTY_POINT'|'GOVERNED_OBJECT_LABEL'|'APPROXIMATE_CONTEXT_BOUNDARY'|'CERTIFIED_OBJECT_BOUNDARY'|'NON_JURISDICTIONAL_CONTEXT_GEOMETRY'|'PROPERTY_RELATIONSHIP_OVERLAY'|'CUSTOMER_SELECTED_MAP_CONTEXT';
export type MapRelationshipInput = Readonly<{ role: MapDisplayRole; identity: boolean; rights: boolean; fresh: boolean; certifiedGeometry: boolean; versionedMethod: boolean; propertyRelationshipEligible: boolean; conflict: boolean; retired: boolean; requestedAssignment: boolean; prohibitedUse: boolean }>;
export type MapRelationshipResult = Readonly<{ visibility:'ADMIN_ONLY'|'DATA_INSUFFICIENT'|'COMPLIANCE_BLOCKED'; authority:'ORIENTATION_ONLY'|'CERTIFIED_BOUNDARY'; activationState:'NOT_AUTHORIZED'; overlayActive:false; reasons:readonly string[] }>;
export function evaluateMapRelationship(input:MapRelationshipInput):MapRelationshipResult { const r:string[]=[]; if(!input.identity||!input.rights||!input.fresh)r.push('SOURCE_READINESS_REQUIRED'); if(input.role==='APPROXIMATE_CONTEXT_BOUNDARY'||input.role==='PROVIDER_REPORTED_COORDINATE'||input.role==='CUSTOMER_SELECTED_MAP_CONTEXT')r.push('DISPLAY_NOT_AUTHORITY'); if(input.role==='CERTIFIED_OBJECT_BOUNDARY'&&(!input.certifiedGeometry||!input.versionedMethod))r.push('CERTIFIED_GEOMETRY_REQUIRED'); if(input.role==='PROPERTY_RELATIONSHIP_OVERLAY'&&!input.propertyRelationshipEligible)r.push('OVERLAY_CANNOT_CREATE_RELATIONSHIP'); if(input.conflict)r.push('CONFLICT_REQUIRES_REVIEW'); if(input.retired)r.push('RETIRED'); if(input.requestedAssignment)r.push('MAP_FILTER_NOT_ASSIGNMENT'); if(input.prohibitedUse)r.push('FAIR_HOUSING_USE_NOT_AUTHORIZED'); return {visibility:input.prohibitedUse?'COMPLIANCE_BLOCKED':r.length?'DATA_INSUFFICIENT':'ADMIN_ONLY',authority:input.role==='CERTIFIED_OBJECT_BOUNDARY'&&!r.length?'CERTIFIED_BOUNDARY':'ORIENTATION_ONLY',activationState:'NOT_AUTHORIZED',overlayActive:false,reasons:r}; }
